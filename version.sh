# Версия сборки — единственное место, где она задаётся.
#
# Раньше она была продублирована в build.sh, build-epk.sh и provenance.sh, и
# смена версии в одном месте роняла сборку в другом. Все три скрипта берут её
# отсюда.
#
# versionName/versionCode в AndroidManifest.xml правятся вручную: aapt читает
# манифест напрямую, и подставлять туда значение из шелла было бы лишним
# слоем, который сам может разъехаться.
VERSION="0.7-live-test"
